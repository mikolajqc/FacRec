<Window x:Class="Client.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:client="clr-namespace:Client"
        mc:Ignorable="d"
        Title="Face Recognition v0.2" Height="480" Width="1200"
        >
    <Window.DataContext>
        <client:MainWindowViewModel/>
    </Window.DataContext>


    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="60" Width="*" MaxWidth="100"/>
            <ColumnDefinition MinWidth="60" Width="*"/>
            <ColumnDefinition MinWidth="60" Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="60" Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Height="30"  Grid.Column="0" Grid.Row="0" x:Name="Snapshot" Content="Snapshot" />
            <Button Height="30"  Grid.Column="0" Grid.Row="1" x:Name="Recognize" Content="Recognize" />
            <Button Height="30"  Grid.Column="0" Grid.Row="2" x:Name="PhotoOfNewFace" Content="Photo of new face" />
            <Button Height="30"  Grid.Column="0" Grid.Row="3" x:Name="AddFace" Content="AddFace" />
            <TextBox Height="30"  Grid.Column="0" Grid.Row="4" x:Name="NameOfUser" Margin="0,16,0,10"/>
            <TextBox Height="30"  Grid.Column="0" Grid.Row="5" x:Name="NumberOfPhotosToAdd" Margin="0,16,0,10"/>
        </Grid>

        <Image Grid.Column="1" x:Name="ImageWebcam" />

        <Grid Grid.Column="2" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="60" Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Image Grid.Column="0" Grid.Row="0"  x:Name="ImageSnapshot"/>
            <ListBox Grid.Column="0" Grid.Row="1" ItemsSource="{Binding ImagesToAdd }"
                     Margin="0,0,0,16">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel MaxWidth="{Binding ActualWidth, ElementName=Panel}" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Image Margin="1" Width="80" Height="120" Source="{Binding}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>

    </Grid>
</Window>
